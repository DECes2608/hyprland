#---------------------Hyperland--------------------#

#
# nvidia
#
env = LIBVA_DRIVER_NAME,nvidia
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = GBM_BACKEND,nvidia-drm
env = __VK_LAYER_NV_optimus,NVIDIA_only
env = QT_QPA_PLATFORMTHEME,qt5ct

# ──────────────────────────────────────────────────────
# OTOMATİK BAŞLAT
# ──────────────────────────────────────────────────────
exec-once = copyq
exec-once = xwayland-satellite
exec-once = qs -c noctalia-shell
exec-once = hyprpaper

# ──────────────────────────────────────────────────────
# MONİTÖRLER
# ──────────────────────────────────────────────────────
env = __GL_GSYNC_ALLOWED,0
env = __GL_VRR_ALLOWED,0
env = QT_QPA_PLATFORMTHEME,qt5ct

#monitor = HDMI-A-1, 1920x1080@60, 0x0, 1
monitor = eDP-1,    1920x1080@60, 1920x0, 1

# ──────────────────────────────────────────────────────
# GİRİŞ
# ──────────────────────────────────────────────────────
input {
    kb_layout    = tr
    repeat_rate  = 35
    repeat_delay = 200
    numlock_by_default = true

    follow_mouse = 1
    mouse_refocus = false

    touchpad {
        tap-to-click         = true
        natural_scroll       = true
        disable_while_typing = false
    }

    sensitivity = -0.5   # mouse accel-speed
}

# Trackpoint / libinput için ek ayarlar (isteğe bağlı):
# device {
#     name = <trackpoint-device-name>
#     sensitivity = 0.2
#     accel_profile = flat
#     scroll_method = on_button_down
#     scroll_button = 273
#     scroll_button_lock = true
#     middle_button_emulation = true
#     natural_scroll = true
# }

# ──────────────────────────────────────────────────────
# GENEL GÖRÜNÜM
# ──────────────────────────────────────────────────────
general {
    gaps_in          = 4
    gaps_out         = 8
    border_size      = 2
    col.active_border   = rgba(464b54ff)
    col.inactive_border = rgba(000000ff)

    layout = dwindle
}

decoration {
    rounding = 10

    }
shadow = false

animations {
    enabled = true
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    pseudotile      = false
    preserve_split  = true
}

misc {
    disable_hyprland_logo = true
    force_default_wallpaper = 0
}

# ──────────────────────────────────────────────────────
# TUŞLAR (keybinds)
# ── SUPER = niri'deki Mod ──────────────────────────────
# ──────────────────────────────────────────────────────

# Uygulama kısayolları
bind = SUPER,       Return,    exec, kitty
bind = SUPER,       D,         exec, rofi -show drun
#bind = SUPER,       B,         exec, zen-browser
#bind = SUPER,       B,         exec, brave
#bind = SUPER,       B,         exec, firefox
bind = SUPER,       B,         exec, helium-browser
bind = SUPER,       M,         exec, spotify-launcher
bind = SUPER,       N,         exec, obsidian
bind = SUPER,       E,         exec, thunar
bind = SUPER,       S,         exec, steam
bind = SUPER,       V,         exec, copyq show
bind = SUPER SHIFT, S,         exec, kitty -e vim ~/.config/hypr/hyprland.conf

# Ekran kilidi
bind = SUPER ALT,   L,         exec, swaylock

# Ses
bindl = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 0.1+ -l 1.0
bindl = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 0.1-
bindl = ,XF86AudioMute,        exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindl = ,XF86AudioMicMute,     exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindl = ,XF86AudioPlay,        exec, playerctl play-pause
bindl = ,XF86AudioStop,        exec, playerctl stop
bindl = ,XF86AudioPrev,        exec, playerctl previous
bindl = ,XF86AudioNext,        exec, playerctl next

# Parlaklık
bind = ,XF86MonBrightnessUp,   exec, brightnessctl --class=backlight set +10%
bind = ,XF86MonBrightnessDown, exec, brightnessctl --class=backlight set 10%-

# Ekran görüntüsü
bind = , Print, exec, grim -g "$(slurp)" - | wl-copy

# Pencere yönetimi
bind = SUPER,       Q,     killactive
bind = SUPER,       F,     fullscreen, 1        # maximize (niri: maximize-column)
bind = SUPER SHIFT, F,     fullscreen, 0        # tam ekran
bind = SUPER,       Space, togglefloating

# Odaklanma — yön tuşları
bind = SUPER, Left,  movefocus, l
bind = SUPER, Right, movefocus, r
bind = SUPER, Up,    movefocus, u
bind = SUPER, Down,  movefocus, d

# Odaklanma — HJKL
bind = SUPER, H, movefocus, l
bind = SUPER, J, movefocus, d
bind = SUPER, K, movefocus, u
bind = SUPER, L, movefocus, r

# Odaklanma — Z/X
bind = SUPER, Z, movefocus, l
bind = SUPER, X, movefocus, r

# Pencere taşıma — yön tuşları
bind = SUPER CTRL, Left,  movewindow, l
bind = SUPER CTRL, Right, movewindow, r
bind = SUPER CTRL, Up,    movewindow, u
bind = SUPER CTRL, Down,  movewindow, d

# Pencere taşıma — HJKL
bind = SUPER CTRL, H, movewindow, l
bind = SUPER CTRL, J, movewindow, d
bind = SUPER CTRL, K, movewindow, u
bind = SUPER CTRL, L, movewindow, r

# Pencere taşıma — Z/X
bind = SUPER SHIFT, Z, movewindow, l
bind = SUPER SHIFT, X, movewindow, r

# Monitör odağı
bind = SUPER SHIFT, Left,  focusmonitor, l
bind = SUPER SHIFT, Right, focusmonitor, r
bind = SUPER SHIFT, Up,    focusmonitor, u
bind = SUPER SHIFT, Down,  focusmonitor, d
bind = SUPER SHIFT, H,     focusmonitor, l
bind = SUPER SHIFT, J,     focusmonitor, d
bind = SUPER SHIFT, K,     focusmonitor, u
bind = SUPER SHIFT, L,     focusmonitor, r

# Pencereyi monitöre taşı
bind = SUPER CTRL SHIFT, Left,  movewindow, mon:l
bind = SUPER CTRL SHIFT, Right, movewindow, mon:r
bind = SUPER CTRL SHIFT, Up,    movewindow, mon:u
bind = SUPER CTRL SHIFT, Down,  movewindow, mon:d
bind = SUPER CTRL SHIFT, H,     movewindow, mon:l
bind = SUPER CTRL SHIFT, J,     movewindow, mon:d
bind = SUPER CTRL SHIFT, K,     movewindow, mon:u
bind = SUPER CTRL SHIFT, L,     movewindow, mon:r

# Workspace geçişi
bind = SUPER, Page_Down, workspace, e+1
bind = SUPER, Page_Up,   workspace, e-1
bind = SUPER, U,         workspace, e+1
bind = SUPER, I,         workspace, e-1

# Pencereyi workspace'e taşı
bind = SUPER CTRL, Page_Down, movetoworkspace, e+1
bind = SUPER CTRL, Page_Up,   movetoworkspace, e-1
bind = SUPER CTRL, U,         movetoworkspace, e+1
bind = SUPER CTRL, I,         movetoworkspace, e-1

# Mouse scroll ile workspace
bind = SUPER, mouse_down, workspace, e+1
bind = SUPER, mouse_up,   workspace, e-1

# Workspace 1–10 geçişi
bind = SUPER, 1, workspace, 1
bind = SUPER, 2, workspace, 2
bind = SUPER, 3, workspace, 3
bind = SUPER, 4, workspace, 4
bind = SUPER, 5, workspace, 5
bind = SUPER, 6, workspace, 6
bind = SUPER, 7, workspace, 7
bind = SUPER, 8, workspace, 8
bind = SUPER, 9, workspace, 9
bind = SUPER, 0, workspace, 10

# Pencereyi workspace'e taşı (1–10)
bind = SUPER SHIFT, 1, movetoworkspace, 1
bind = SUPER SHIFT, 2, movetoworkspace, 2
bind = SUPER SHIFT, 3, movetoworkspace, 3
bind = SUPER SHIFT, 4, movetoworkspace, 4
bind = SUPER SHIFT, 5, movetoworkspace, 5
bind = SUPER SHIFT, 6, movetoworkspace, 6
bind = SUPER SHIFT, 7, movetoworkspace, 7
bind = SUPER SHIFT, 8, movetoworkspace, 8
bind = SUPER SHIFT, 9, movetoworkspace, 9
bind = SUPER SHIFT, 0, movetoworkspace, 10

# Boyut değiştirme (resizeactive ile)
binde = SUPER,       minus, resizeactive, -10% 0
binde = SUPER,       equal, resizeactive, 10% 0
binde = SUPER SHIFT, minus, resizeactive, 0 -10%
binde = SUPER SHIFT, equal, resizeactive, 0 10%

# Çıkış / Güç
bind = SUPER SHIFT, ESCAPE, exec, hyprshutdown -t 'Shutting down...' --post-cmd 'shutdown -P 0'
bind = SUPER SHIFT, Q, exec, hyprshutdown -vt 2 'Restarting...' --post-cmd 'reboot'

# Fare / Tema (bu kısım daha çok farenin görünümüyle ilgili çok önemli değil)
#env = HYPRCURSOR_THEME,PKP
#env = HYPRCURSOR_SIZE,24
#exec-once = hyprctl setcursor PKP 24

# Pencere Taşıma
bindm = SUPER, mouse:272, movewindow
bindm = SUPER, mouse:273, resizewindow

# Pencere Kuralları
#windowrule = workspace 3, class:brave-browser
#windowrule = workspace 2, class:obsidian
